# Digital Signature Algorithm Performance Analysis
# Project Dependencies

# Classical Cryptography
# - Baseline benchmarks: cryptography library (Python wrapper)
# - Parallelism benchmarks: Direct OpenSSL bindings via CFFI (GIL-releasing)
cryptography>=42.0.0
cffi>=1.16.0

# Post-Quantum Cryptography (Dilithium, SPHINCS+)
# Note: Requires liboqs system library installed
# macOS: brew install liboqs
# Linux: sudo apt install liboqs-dev
liboqs-python>=0.14.0

# Data Analysis and Statistics
numpy>=2.0.0

# System Telemetry (CPU, memory, power monitoring)
psutil>=5.9.0
